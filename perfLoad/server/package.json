{
  "name": "server",
  "private": true,
  "type": "module",
  "main": "servers.ts",
  "scripts": {
    "dev": "node --watch servers.ts",
    "start": "node servers.ts",
    "typecheck": "tsc --noEmit",
    "cluster:demo": "node clusterTest.ts"
  },
  "dependencies": {
    "@perf/contract": "*",
    "@socket.io/cluster-adapter": "^0.3.0",
    "@socket.io/sticky": "^2.0.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
